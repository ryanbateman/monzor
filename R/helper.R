testToken <- "<Token>
<oauth_endpoint>
authorize: https://auth.monzo.com/
access:    https://api.monzo.com/oauth2/token
<oauth_app> monzor
key:    nothing
secret: nothing
<credentials> access_token, client_id, expires_in, refresh_token, scope, token_type, user_id"
